using System;
using System.Windows;

namespace TRPG.ManagementConsole
{
    internal class Program
    {
        [STAThread]
        private static void Main()
        {
            //Create an instance of your window.
            //var window = new LibraryWindow();
            //Create an instance of a new Application
            //var wpfApplication = new Application();
            //Run this Application by passing the window object as the argument
            //wpfApplication.Run(window);
        }
    }
}